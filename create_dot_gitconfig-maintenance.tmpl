# This file contains the global configuration for 'git maintenance'.
# As of 2025-01-15 this is just the list of repositories in this machine that are configured
# for automatic maintenance.
# It is included from the main .gitconfig, but kept separate so chezmoi doesn't keep encountering
# changes in .gitconfig which are different in each machine.
